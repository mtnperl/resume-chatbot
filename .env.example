# Anthropic API Key (required)
# Get yours at https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Vercel KV (required for rate limiting in production)
# Set these up at https://vercel.com/storage/kv
# Leave blank in local dev — rate limiting will be disabled (fail open)
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# PR 2: Admin dashboard (added in next PR)
# ADMIN_PASSWORD=your_admin_password_here

# Access gate (mathanperl.com landing → /hub)
# APP_PASSWORD: shared password users enter on the landing page.
# APP_SESSION_SECRET: 32+ char random string used to sign the session cookie.
#   Generate with: openssl rand -hex 32
APP_PASSWORD=
APP_SESSION_SECRET=
